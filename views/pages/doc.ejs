<div class="container mt-5">
    <h1 class="mb-4">Sails.js API Documentation</h1>
  
    <p>Esta é a documentação dos endpoints da API Sails.js utilizada neste projeto.</p>
  
    <h2>Endpoints</h2>
  
    <!-- User Endpoints -->
    <h3>Endpoints para o modelo User</h3>
    
    <h4>POST /users</h4>
    <p>Este endpoint cria um novo usuário com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /users</h4>
    <p>Este endpoint retorna uma lista de todos os usuários cadastrados no sistema.</p>
    
    <h4>GET /users/:id</h4>
    <p>Este endpoint retorna os detalhes de um usuário específico com o ID fornecido na URL.</p>
    
    <h4>PUT /users/:id</h4>
    <p>Este endpoint atualiza os dados de um usuário específico com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /users/:id</h4>
    <p>Este endpoint exclui um usuário específico com o ID fornecido na URL.</p>
  
    <!-- Users Forms Endpoints -->
    <h3>Endpoints para o modelo Users_forms</h3>
    
    <h4>POST /users-forms</h4>
    <p>Este endpoint cria uma nova entrada no modelo Users_forms com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /users-forms</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Users_forms cadastradas no sistema.</p>
    
    <h4>GET /users-forms/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Users_forms com o ID fornecido na URL.</p>
    
    <h4>PUT /users-forms/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Users_forms com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /users-forms/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Users_forms com o ID fornecido na URL.</p>
  
    <!-- General Forms Endpoints -->
    <h3>Endpoints para o modelo General_forms</h3>
    
    <h4>POST /general-forms</h4>
    <p>Este endpoint cria uma nova entrada no modelo General_forms com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /general-forms</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo General_forms cadastradas no sistema.</p>
    
    <h4>GET /general-forms/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo General_forms com o ID fornecido na URL.</p>
    
    <h4>PUT /general-forms/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo General_forms com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /general-forms/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo General_forms com o ID fornecido na URL.</p>
  
    <!-- Have Forms Endpoints -->
    <h3>Endpoints para o modelo Have_forms</h3>
    
    <h4>POST /have-forms</h4>
    <p>Este endpoint cria uma nova entrada no modelo Have_forms com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /have-forms</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Have_forms cadastradas no sistema.</p>
    
    <h4>GET /have-forms/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Have_forms com o ID fornecido na URL.</p>
    
    <h4>PUT /have-forms/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Have_forms com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /have-forms/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Have_forms com o ID fornecido na URL.</p>
  
    <!-- Had Forms Endpoints -->
    <h3>Endpoints para o modelo Had_forms</h3>
    
    <h4>POST /had-forms</h4>
    <p>Este endpoint cria uma nova entrada no modelo Had_forms com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /had-forms</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Had_forms cadastradas no sistema.</p>
    
    <h4>GET /had-forms/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Had_forms com o ID fornecido na URL.</p>
    
    <h4>PUT /had-forms/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Had_forms com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /had-forms/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Had_forms com o ID fornecido na URL.</p>
  
    <!-- Want Forms Endpoints -->
    <h3>Endpoints para o modelo Want_forms</h3>
    
    <h4>POST /want-forms</h4>
    <p>Este endpoint cria uma nova entrada no modelo Want_forms com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /want-forms</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Want_forms cadastradas no sistema.</p>
    
    <h4>GET /want-forms/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Want_forms com o ID fornecido na URL.</p>
    
    <h4>PUT /want-forms/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Want_forms com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /want-forms/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Want_forms com o ID fornecido na URL.</p>
  
    <!-- Null Forms Endpoints -->
    <h3>Endpoints para o modelo Null_forms</h3>
    
    <h4>POST /null-forms</h4>
    <p>Este endpoint cria uma nova entrada no modelo Null_forms com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /null-forms</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Null_forms cadastradas no sistema.</p>
    
    <h4>GET /null-forms/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Null_forms com o ID fornecido na URL.</p>
    
    <h4>PUT /null-forms/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Null_forms com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /null-forms/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Null_forms com o ID fornecido na URL.</p>
  
    <!-- Dog Forms Have Endpoints -->
    <h3>Endpoints para o modelo Dog_forms_have</h3>
    
    <h4>POST /dog-forms-have</h4>
    <p>Este endpoint cria uma nova entrada no modelo Dog_forms_have com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /dog-forms-have</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Dog_forms_have cadastradas no sistema.</p>
    
    <h4>GET /dog-forms-have/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Dog_forms_have com o ID fornecido na URL.</p>
    
    <h4>PUT /dog-forms-have/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Dog_forms_have com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /dog-forms-have/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Dog_forms_have com o ID fornecido na URL.</p>
  
    <!-- Dog Forms Want Endpoints -->
    <h3>Endpoints para o modelo Dog_forms_want</h3>
    
    <h4>POST /dog-forms-want</h4>
    <p>Este endpoint cria uma nova entrada no modelo Dog_forms_want com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /dog-forms-want</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo Dog_forms_want cadastradas no sistema.</p>
    
    <h4>GET /dog-forms-want/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo Dog_forms_want com o ID fornecido na URL.</p>
    
    <h4>PUT /dog-forms-want/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo Dog_forms_want com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /dog-forms-want/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo Dog_forms_want com o ID fornecido na URL.</p>
  
    <!-- All Dog Endpoints -->
    <h3>Endpoints para o modelo All_Dog</h3>
    
    <h4>POST /all-dogs</h4>
    <p>Este endpoint cria uma nova entrada no modelo All_Dog com os dados fornecidos no corpo da requisição.</p>
    
    <h4>GET /all-dogs</h4>
    <p>Este endpoint retorna uma lista de todas as entradas do modelo All_Dog cadastradas no sistema.</p>
    
    <h4>GET /all-dogs/:id</h4>
    <p>Este endpoint retorna os detalhes de uma entrada específica do modelo All_Dog com o ID fornecido na URL.</p>
    
    <h4>PUT /all-dogs/:id</h4>
    <p>Este endpoint atualiza os dados de uma entrada específica do modelo All_Dog com o ID fornecido na URL. Os novos dados são fornecidos no corpo da requisição.</p>
    
    <h4>DELETE /all-dogs/:id</h4>
    <p>Este endpoint exclui uma entrada específica do modelo All_Dog com o ID fornecido na URL.</p>
  
    <h2>Exemplos de Uso</h2>
  
    <p>Aqui estão alguns exemplos de como você pode utilizar os endpoints da API:</p>
  
    <pre>
  GET /users
  - Retorna todos os usuários cadastrados no sistema.
  
  GET /users/1
  - Retorna os detalhes do usuário com ID 1.
  
  POST /users
  - Cria um novo usuário com os seguintes dados no corpo da requisição:
    {
      "nome": "Maria",
      "email": "maria@example.com"
    }
  
  PUT /users/1
  - Atualiza os dados do usuário com ID 1 com os seguintes dados no corpo da requisição:
    {
      "nome": "Maria Silva",
      "email": "maria.silva@example.com"
    }
  
  DELETE /users/1
  - Exclui o usuário com ID 1 do sistema.
      </pre>
  </div>
  

  